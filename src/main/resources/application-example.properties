# Copia este archivo como application-local.properties y completa los valores
# application-local.properties está en .gitignore — nunca subas contraseñas al repo

server.port=8082
spring.datasource.url=jdbc:postgresql://localhost:5432/pedidos_db
spring.datasource.username=postgres
spring.datasource.password=TU_PASSWORD_AQUI
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
